# Optional — users can also enter their key in the UI per audit.
# Only set this if you want AI recommendations enabled by default for all audits.
OPENAI_API_KEY=
MAX_PAGES=50
MAX_DEPTH=3
LIGHTHOUSE_PAGES=5
OPENAI_MODEL=gpt-4o-mini
